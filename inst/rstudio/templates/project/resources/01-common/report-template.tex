\documentclass[letterpaper,11pt,$for(classoption)$$classoption$$sep$,$endfor$]{article}

% packages for using helvetica as the font
\usepackage{sansmathfonts}
\usepackage{helvet}
\renewcommand{\rmdefault}{\sfdefault}

% math packages, not sure where this came from...
\usepackage{amssymb,amsmath}
\usepackage[T1]{fontenc}
\usepackage[utf8]{inputenc}
\IfFileExists{microtype.sty}{%
\usepackage{microtype}
\UseMicrotypeSet[protrusion]{basicmath} % disable protrusion for tt fonts
}{}

% set page margins
\usepackage[top=0.5in,right=0.75in,bottom=0.75in,left=0.75in]{geometry}

% settings for hyperlinks
\usepackage{hyperref}
\hypersetup{unicode=true,pdfborder={0 0 0},breaklinks=true,colorlinks=true,urlcolor=blue}
\urlstyle{same}  % don't use monospace font for urls

% load setspace package
\usepackage{setspace}

% settings for figures. Not sure where this came from...
\usepackage{graphicx,grffile}
\makeatletter
\def\maxwidth{\ifdim\Gin@nat@width>\linewidth\linewidth\else\Gin@nat@width\fi}
\def\maxheight{\ifdim\Gin@nat@height>\textheight\textheight\else\Gin@nat@height\fi}
\makeatother
% Scale images if necessary, so that they will not overflow the page
% margins by default, and it is still possible to overwrite the defaults
% using explicit options in \includegraphics[width, height, ...]{}
\setkeys{Gin}{width=\maxwidth,height=\maxheight,keepaspectratio}

\graphicspath{{$graphics-path$}}

% spacing set up: not sure where this came from
\IfFileExists{parskip.sty}{%
\usepackage{parskip}
}{% else
\setlength{\parindent}{0pt}
\setlength{\parskip}{6pt plus 2pt minus 1pt}
}
\setlength{\emergencystretch}{3em}  % prevent overfull lines
\providecommand{\tightlist}{%
\setlength{\itemsep}{0pt}\setlength{\parskip}{0pt}}
\setcounter{secnumdepth}{5}

\usepackage{titlesec}
\titlespacing\section{0pt}{6pt plus 4pt minus 2pt}{6pt plus 4pt minus 2pt}
\titlespacing\subsection{0pt}{6pt plus 4pt minus 2pt}{6pt plus 4pt minus 2pt}
\titlespacing\subsubsection{0pt}{6pt plus 4pt minus 2pt}{6pt plus 4pt minus 2pt}

%%% Use protect on footnotes to avoid problems with footnotes in titles
% not sure where this came from
\let\rmarkdownfootnote\footnote%
\def\footnote{\protect\rmarkdownfootnote}
\usepackage[hang,flushmargin]{footmisc}

% set up captions
\usepackage{microtype}
\usepackage[labelfont={bf,sc},labelsep=period,font=small,figureposition=bottom]{caption}
\captionsetup{width=\textwidth,justification=raggedright,singlelinecheck=false}
\usepackage{floatrow}
\floatsetup[figure]{capposition=top}
\floatsetup[table]{capposition=top}

% packages for kableExtra
\usepackage{booktabs}
\usepackage{longtable}
\usepackage{array}
\usepackage{multirow}
\usepackage[table]{xcolor}
\usepackage{wrapfig}
\usepackage{float}
\usepackage{colortbl}
\usepackage{pdflscape}
\usepackage{tabu}
\usepackage{threeparttable}
\usepackage{threeparttablex}
\usepackage[normalem]{ulem}
\usepackage{makecell}

% header information
\usepackage{fancyhdr}
\setlength{\headheight}{13.59999pt}
\pagestyle{fancy}
\cfoot{\thepage}
\rfoot{\textit{\small{$rfooter$}}}
\lfoot{\textit{\small{$lfooter$}}}
\rhead{\empty}
\renewcommand{\headrulewidth}{0pt}  % make width of the rulers zero
\renewcommand{\footrulewidth}{0.5pt}

% add a watermark if requested
$if(draft-watermark)$
\usepackage{draftwatermark}
\SetWatermarkText{Draft}
\SetWatermarkScale{1}
\SetWatermarkColor[gray]{0.9}
\SetWatermarkAngle{315}
$endif$

% start rendered output of document
\begin{document}

% insert title
\rule{\textwidth}{1pt}
\begin{flushleft}

{\bf\LARGE{$title$}}\\
{\Large{$opener-label$}}\\

{\small{Opportunity Time Period: $opener-start$ -- $opener-end$ ($opener-duration$)}}\\

{\small{Area Covered by Estimates: $ds-bound$ \( \longleftrightarrow \) $us-bound$}}\\

$if(contact)$
{\small{Contact Person(s): $contact$}}\\
$endif$

$if(special-action)$
{\small{Special Action \#: $special-action$}}\\
$endif$

$if(special-action-url)$
{\small{Special Action: $special-action-url$}}\\
$endif$

$if(news-release-url)$
{\small{News Release: $news-release-url$}}\\
$endif$
\end{flushleft}

% add logos
\begin{minipage}[ht]{0.33\textwidth}
\centering
\includegraphics{onc-logo}
\end{minipage}
\begin{minipage}[ht]{0.33\textwidth}
\centering
\includegraphics{kritfc-logo}
\end{minipage}
\begin{minipage}[ht]{0.33\textwidth}
\centering
\includegraphics{usfws-logo}
\end{minipage}

% \vspace{-10pt}
\rule{\textwidth}{1pt}

% insert body
$body$

% end the document
\end{document}
