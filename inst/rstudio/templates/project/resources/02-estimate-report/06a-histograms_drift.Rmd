
```{r histograms, fig.width = 7.5, fig.height = 4, fig.cap = KuskoHarvEst:::make_histogram_caption(interview_data, "drift")}
make_histograms(interview_data, gear = "drift", variables = c("total_salmon", "chinook", "chum+sockeye", "trip_duration", "soak_duration", "p_chinook"), n_bins = 10)
```
