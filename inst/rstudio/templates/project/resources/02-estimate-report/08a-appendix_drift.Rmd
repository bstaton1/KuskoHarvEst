
\clearpage
\setcounter{figure}{0}
\setcounter{table}{0}
\renewcommand\thefigure{A\arabic{figure}}
\renewcommand\thetable{A\arabic{table}}

# Appendix: Detailed Interview Summaries {-}

**Column Meanings**

* **Area**: the area of the river the trip occurred in
* **N**: the number of interviews with usable information in each area
* **Min**: the minimum value among all trips in each area
* **25%**: the value that 25% of the trips fell below in each area
* **Mean**: the average value across all trips in each area
* **75%**: the value that 75% of the trips fell below in each area
* **Max**: the maximum value among all trips in each area

_Information is for drift net trips only._

```{r chinook-rate-appendix-table}
appendix_table(interview_data, "drift", "chinook_rate")
```

```{r chinook-appendix-table}
appendix_table(interview_data, "drift", "chinook")
```

```{r chum-sockeye-rate-appendix-table}
appendix_table(interview_data, "drift", "chum+sockeye_rate")
```

```{r chum-sockeye-appendix-table}
appendix_table(interview_data, "drift", "chum+sockeye")
```

```{r p-chinook-appendix-table}
appendix_table(interview_data, "drift", "p_chinook")
```

```{r soak-duration-appendix-table}
appendix_table(interview_data, "drift", "soak_duration")
```

```{r trip-duration-appendix-table}
appendix_table(interview_data, "drift", "trip_duration")
```

```{r trip-start-appendix-table}
appendix_table(interview_data, "drift", "trip_start")
```

```{r trip-end-appendix-table}
appendix_table(interview_data, "drift", "trip_end")
```
