\clearpage
\setcounter{figure}{0}
\setcounter{table}{0}
\renewcommand\thefigure{NONSALMON_APPENDIX_REPLACE\arabic{figure}}
\renewcommand\thetable{NONSALMON_APPENDIX_REPLACE\arabic{table}}

# Appendix NONSALMON_APPENDIX_REPLACE: Non-salmon Harvest Information {-}

```{r estimate-harvest-nonsalmon}
# estimate (via bootstrap) set net harvest for all species and and strata
boot_out_set = bootstrap_harvest(interview_data, set_effort_info,
                                 gear = "set", n_boot = params$n_boot, stratify_interviews = FALSE, nonsalmon = TRUE)

# don't perform a drift net estimate

# combine drift, set, and total into one data frame of bootstrap output
boot_out <<- combine_boot_out(boot_out_drift = NULL, boot_out_set = boot_out_set)
```

```{r harvest-bullets-nonsalmon, results = "asis"}
harvest_bullets(include_set_summary = FALSE, nonsalmon = TRUE)
```

```{r}
make_strata_summary_table(interview_data, "set", nonsalmon = TRUE)
```

```{r, results = "asis"}
make_appendix_tables(interview_data, gear = "set", include_rates = FALSE, extra = NULL, nonsalmon = TRUE)
```
