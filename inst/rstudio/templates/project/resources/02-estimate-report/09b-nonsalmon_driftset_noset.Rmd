
```{r}
appendix_counter = appendix_counter + 1
```

\clearpage
\setcounter{figure}{0}
\setcounter{table}{0}
\renewcommand\thefigure{`r LETTERS[appendix_counter]`\arabic{figure}}
\renewcommand\thetable{`r LETTERS[appendix_counter]`\arabic{table}}

# Appendix `r LETTERS[appendix_counter]`: Non-salmon Harvest Information {-}

```{r estimate-harvest-nonsalmon}
# estimate (via bootstrap) drift net harvest for all species and and strata
boot_out_drift = bootstrap_harvest(interview_data, drift_effort_info,
                                   gear = "drift", n_boot = params$n_boot, stratify_interviews = TRUE, nonsalmon = TRUE)

# don't perform a set net estimate

# combine drift, set, and total into one data frame of bootstrap output
boot_out <<- combine_boot_out(boot_out_drift = boot_out_drift, boot_out_set = NULL)
```

```{r harvest-bullets-nonsalmon, results = "asis"}
harvest_bullets(include_set_summary = FALSE, nonsalmon = TRUE)
```

```{r}
make_strata_summary_table(interview_data, "drift", nonsalmon = TRUE)
```

```{r, results = "asis"}
make_appendix_tables(interview_data, gear = "drift", include_rates = FALSE, extra = NULL, nonsalmon = TRUE)
```
